export interface Project {
  id: number;
  name: string;
  description: string;
  frontend: string;
  backend: string;
  tools: string;
  url: string;
  code: string;
  image: string;
  finished: boolean;
}

